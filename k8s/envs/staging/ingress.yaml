apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-app-node
spec:
  rules:
  - host: node-app.staging.example.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: app-node
            port:
              name: node-port